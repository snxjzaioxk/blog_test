source "https://rubygems.org"

# Hello! This is where you manage Jekyll's dependencies.
#
# Add Jekyll's gems to the Gemfile to ensure Jekyll uses the same
# versions of dependency gems on every install. A Gemfile is
# necessary for Jekyll to compile your site.
#
# For more information, see:
#   https://jekyllrb.com/docs/ruby-gems/
#   https://bundler.io/guides/gemfile.html

gem "jekyll", "~> 4.2" # 这行是核心，指定了 Jekyll 本身
# If you want to use GitHub Pages, remove the "gem "jekyll"" line above and
# uncomment the line below. To see which Jekyll gem version is used by GitHub
# Pages, run "bundle info github-pages".
# gem "github-pages", group: :jekyll_plugins

# If you have a theme, uncomment the line below to ensure it's installed.
gem "minima", "~> 2.5" # 默认主题，如果用其他主题，这里会不同

# If you have any other plugins, add them here!
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end

# Windows and JRuby do not include nokogiri by default. To help install
# it, uncomment the line below and install its native dependencies.
# gem "nokogiri"